README.txt
Wsgi:
  app.py -- Holds the routes for the website, connects to the database
  config.py, lost_config.json -- Configurations files

templates:
  holds the template html files for the urls

clients:
  holds the client files to create and revoke users


