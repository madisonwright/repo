README.txt
Wsgi:
  app.py -holds the routs for the stubs
  config.py, lost_config.json -same as last time, configurations files

templates:
  holds the template html files for the urls

clients:
 just stull here

I just didn't have it in me to comment much of my code.  Pluss if I clutter it up at all I won't be able to read it myself.

