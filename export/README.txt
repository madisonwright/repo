
Export_data.sh  Uses a psql Copy function to put the information into csv files, then calls organize_data.py

Organize_data.py  Puts the csv files into the format desired by the step 3 specifications.
